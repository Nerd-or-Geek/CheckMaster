{
  "name": "checkmaster-sync-server",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted sync API for CheckMaster (folders, checklists, settings).",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "bootstrap": "node scripts/bootstrap.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
